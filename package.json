{
  "name": "uinaf-intake",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "lint": "oxlint --ignore-pattern '*.astro'",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "typecheck": "astro check && tsc --noEmit",
    "icons": "bun scripts/icons.ts",
    "check:entries": "bun scripts/validate-entries.ts",
    "check:links": "bun scripts/check-links.ts",
    "intake:find": "bun scripts/find-source.ts",
    "check": "bun run check:entries && bun run lint && bun run typecheck && bun run format:check",
    "verify": "bun run check && bun run build && bun run check:links"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/rss": "^4.0.15",
    "@uinaf/design": "^0.1.0",
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@tailwindcss/vite": "^4.3.2",
    "@types/node": "^26.1.1",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.72.0",
    "png-to-ico": "^3.0.2",
    "satori": "^0.29.0",
    "sharp": "^0.35.3",
    "tailwindcss": "^4.3.2",
    "typescript": "^6.0.3",
    "yaml": "2.9.0"
  },
  "overrides": {
    "fast-uri": "3.1.2",
    "rollup": "4.60.3",
    "smol-toml": "1.6.1",
    "yaml": "2.9.0"
  }
}
