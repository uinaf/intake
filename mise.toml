[settings]
jobs = 4

[tasks."check:entries"]
description = "Validate intake entries and source uniqueness"
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  "entries/**",
  "scripts/entry-*.json",
  "scripts/entry-schema.ts",
  "scripts/markdown-safety.ts",
  "scripts/validate-entries.ts",
  "src/content.config.ts",
  "src/lib/entry-body.ts",
  "package.json",
  "pnpm-lock.yaml",
]
run = "pnpm run check:entries"

[tasks."check:tests"]
description = "Run Node contract tests"
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  "src/**/*.ts",
  "scripts/**/*.ts",
  "package.json",
  "pnpm-lock.yaml",
]
run = "pnpm run test"

[tasks."check:lint"]
description = "Lint JavaScript and TypeScript sources"
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  "src/**",
  "scripts/**",
  "*.ts",
  "*.mjs",
  "package.json",
  "pnpm-lock.yaml",
]
run = "pnpm run lint"

[tasks."check:astro"]
description = "Check Astro content and component types"
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  "entries/**",
  "src/**",
  "scripts/**",
  "astro.config.ts",
  "tsconfig.json",
  "package.json",
  "pnpm-lock.yaml",
]
run = "pnpm run typecheck:astro"

[tasks."check:tsc"]
description = "Typecheck TypeScript sources without writing output"
depends = ["check:astro"]
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  "src/**",
  "scripts/**",
  "astro.config.ts",
  "tsconfig.json",
  "package.json",
  "pnpm-lock.yaml",
]
run = "pnpm run typecheck:tsc"

[tasks."check:typecheck"]
description = "Typecheck Astro and TypeScript sources"
depends = ["check:tsc"]

[tasks."check:format"]
description = "Check repository formatting"
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  ".github/**/*.yml",
  ".skillcheck/**",
  "entries/**",
  "skills/**",
  "src/**",
  "scripts/**",
  "*.json",
  "*.jsonc",
  "*.md",
  "*.ts",
  "*.yaml",
]
run = "pnpm run format:check"

[tasks."check:design"]
description = "Check design-system usage"
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  "src/**",
  "package.json",
  "pnpm-lock.yaml",
]
run = "pnpm run design:check"

[tasks.check]
description = "Run affected static and test lanes in parallel"
depends = [
  "check:entries",
  "check:tests",
  "check:lint",
  "check:typecheck",
  "check:format",
  "check:design",
]

[tasks.build]
description = "Build the site when build inputs change"
depends = ["check:typecheck"]
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  ".brand-cache/**",
  "entries/**",
  "public/**",
  "src/**",
  "astro.config.ts",
  "tsconfig.json",
  "package.json",
  "pnpm-lock.yaml",
]
outputs = ["dist/**"]
run = "pnpm run build"

[tasks."build:budget"]
description = "Check generated search-index size"
depends = ["build"]
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  "dist/**",
  "scripts/check-build-budget.ts",
]
run = "pnpm run check:build-budget"

[tasks."build:links"]
description = "Check generated internal links"
depends = ["build"]
sources = ["mise.toml", ".node-version", ".tool-versions", "dist/**", "scripts/check-links.ts"]
run = "pnpm run check:links"

[tasks."build:cloudflare"]
description = "Check the generated Cloudflare deployment contract"
depends = ["build"]
sources = [
  "mise.toml",
  ".node-version",
  ".tool-versions",
  "dist/**",
  "scripts/check-cloudflare.ts",
  "wrangler.jsonc",
]
run = "pnpm run check:cloudflare"

[tasks.verify]
description = "Run affected repository verification"
depends = ["check", "build:budget", "build:links", "build:cloudflare"]
run = "git diff --check && git diff --cached --check"
